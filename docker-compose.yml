services:
  app:
    build: .
    env_file: .env
    ports:
      - "8080:80"
    restart: unless-stopped

  scheduler:
    build: .
    env_file: .env
    command: ["php", "artisan", "schedule:work"]
    restart: unless-stopped
